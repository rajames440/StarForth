\documentclass[10pt,a4paper]{article}

%% ── Packages ──────────────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{parskip}
\usepackage{enumitem}
\usepackage[hidelinks,colorlinks=true,linkcolor=black,citecolor=black,urlcolor=blue]{hyperref}
\usepackage{lmodern}

%% ── Colour palette ────────────────────────────────────────────────────────────
\definecolor{sfblue}{RGB}{31,119,180}
\definecolor{sforange}{RGB}{255,127,14}
\definecolor{sfgreen}{RGB}{44,160,44}
\definecolor{sfgray}{RGB}{80,80,80}

%% ── Figure path ───────────────────────────────────────────────────────────────
\graphicspath{{figures/}}

%% ── Title metadata ────────────────────────────────────────────────────────────
\title{%
  \textbf{LithosAnanke Bare-Metal Design of Experiments}\\[0.4em]
  \large Compudynamic Invariance Across Three\\
  Instruction-Set Architectures
}
\author{%
  R.\,A. James (Captain Bob)\\[0.2em]
  \small StarForth Project \quad\texttt{robert.allan.james@gmail.com}
}
\date{12 June 2026 \quad\textnormal{(Patent Pending --- Provisional Filed December 2025)}}

%% ═════════════════════════════════════════════════════════════════════════════
\begin{document}

\maketitle
\thispagestyle{empty}

%% ── Abstract ──────────────────────────────────────────────────────────────────
\begin{abstract}
\noindent
Execution rate is statistically indistinguishable across three 64-bit
instruction-set architectures (\(F_{2,783285}\approx 0\), \(p=1.000\)) ---
the strongest empirical statement of cross-ISA compudynamic invariance yet
recorded, backed by 783\,288 heartbeat ticks across a full 30-replicate
campaign.
We report the first 30-replicate bare-metal Design of Experiments (DoE)
validation of the StarForth Steady-State Machine (SSM) running inside the
LithosAnanke UEFI microkernel on \textsc{amd64}, \textsc{aarch64}, and
\textsc{riscv64}, each booting from UEFI firmware under QEMU TCG software
emulation.
Thirty fully-replicated trials (480 configuration runs per architecture)
confirm that the SSM's adaptive rolling-window mechanism converges to a
characteristic attractor regardless of the underlying silicon.  The
window-width Poincaré map reveals a limit cycle spanning \{256, 512, 1024,
2048, 4096\} cells --- a five-point discrete orbit we term the
\emph{compudynamic breathing cycle}.
Architecture-specific timer hardware introduces a quantifiable but
attractor-neutral divergence in temporal trust and variance
(\(H=667\,664\), \(p < 2.2 \times 10^{-16}\)): the x86-64 APIC emulation
leaves a small calibration residual ($\bar{t}=0.9947$,
$\bar{\sigma}^2=9.5\times10^{-3}$) absent from the ARMv8 Generic Timer and
RISC-V CLINT.  This divergence does not disturb the attractor.
Notably, \textsc{aarch64} and \textsc{riscv64} produce identical output for
all attractor metrics across all 261\,095 ticks; a single-session byte-identity
of the two original runs independently validates the determinism of the SSM
algorithm, while a clean re-run reveals that heat accumulation has a small
run-to-run variation that does not perturb the attractor.
The physics of the FORTH interpreter is a property of the algorithm, not
the hardware.
\end{abstract}

\bigskip
\hrule
\tableofcontents
\hrule
\bigskip

%% ═════════════════════════════════════════════════════════════════════════════
\section{Introduction}
\label{sec:intro}

LithosAnanke is the bare-metal branch of StarForth~v3.0.1 --- a FORTH-79
compliant virtual machine whose runtime is governed by the \emph{Steady-State
Machine} (SSM), a physics-driven adaptive execution engine.  The SSM monitors
eight feedback loops (L1--L8) in real time, continuously adjusting the
rolling-window width, decay slope, and Jacquard mode selector to maintain
computational equilibrium.

Prior work validated the SSM in a hosted Linux environment via a \(2^7\)
factorial DoE (128 configurations \(\times\) 300 replicates \(= 38\,400\) total
runs).  That campaign established 0\,\% algorithmic variance and identified five
statistically dominant L8 Jacquard modes.  The current paper extends that
validation to the bare-metal setting: LithosAnanke boots directly from UEFI,
manages its own physical and virtual memory, runs no operating-system kernel
beneath it, and fires a hardware APIC/timer interrupt at exactly 10\,\(\mu\)s to
clock the heartbeat.

The central question is: \textbf{do the compudynamic attractors persist across
distinct ISAs?}  A positive answer would establish that the attractors are a
property of the SSM algorithm, not an artefact of any particular processor
micro-architecture.

%% ═════════════════════════════════════════════════════════════════════════════
\section{Experimental Design}
\label{sec:design}

\subsection{System Under Test}

The system under test is LithosAnanke commit~\texttt{lithosananke} (v1.1.0),
built against three targets:
\begin{itemize}[noitemsep]
  \item \texttt{amd64} — x86-64, TCG emulation, OVMF UEFI, APIC 10\,\(\mu\)s interval
  \item \texttt{aarch64} — ARMv8-A (Cortex-A57), TCG emulation, UEFI, Generic Timer
  \item \texttt{riscv64} — RISC-V RV64GC, TCG emulation, OpenSBI~+~UEFI, CLINT
\end{itemize}

All three targets are emulated under QEMU with the \texttt{-nographic -serial stdio}
console; the heartbeat tick interval is fixed at 10\,000\,ns (10\,\(\mu\)s,
100\,kHz) by the kernel's hardware-timer initialisation, making the timer
\emph{deterministic} by construction.

\subsection{Capsule and DoE Configuration}

The DoE is driven by \texttt{capsules/doe.4th}, a FORTH-79 capsule loaded at
boot from block~2056.  The capsule defines a \(2^4 = 16\)-configuration
matrix (factors: entropy threshold, coefficient of variation, temporal decay,
stability flag) and an outer replicate loop.  The entry point is:

\begin{verbatim}
    EXEC-DOE ( seed n-reps -- )
    DOE      ( -- )  12345 3 EXEC-DOE   ( convenience alias )
\end{verbatim}

With \texttt{n-reps = 30} and \texttt{N-CFG = 16}, each architecture executes
\(30 \times 16 = 480\) configuration runs.  Each run drives the heartbeat until
it emits a full tick snapshot to the serial CSV stream.

\begin{table}[h!]
\centering
\caption{DoE parameter matrix}
\label{tab:design}
\begin{tabular}{lll}
\toprule
Parameter & Low & High \\
\midrule
Entropy threshold  & 0.50 & 0.75 \\
Coefficient of variation (CV) & 0.10 & 0.15 \\
Temporal decay & 0.30 & 0.50 \\
Stability flag & 0 & 1 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Data Collection}

Each heartbeat tick emits one CSV row via the kernel serial port.  Rows were
captured from QEMU serial output and stored in
\texttt{experiments/bare\_metal/runs/}.  The row schema is:

\begin{verbatim}
    tick_count, apic_ticks, exec_delta, window_width, heat, hot_word_count,
    time_trust_q48, variance_q48, tick_interval_ns
\end{verbatim}

Q48.16 fixed-point values (\texttt{time\_trust\_q48}, \texttt{variance\_q48})
are divided by 65\,536 during analysis to obtain floating-point equivalents.

\begin{table}[h!]
\centering
\caption{Dataset summary --- 30-replicate campaign}
\label{tab:dataset}
\sisetup{group-separator={,}, group-minimum-digits=4}
\begin{tabular}{lSSSS}
\toprule
Architecture & {Ticks} & {Mean exec/tick} & {Mean $\sigma$(exec)} & {Elapsed (s)} \\
\midrule
amd64   & 261098 & 255.98 & 1.107 & 2.611 \\
aarch64 & 261095 & 255.98 & 1.117 & 2.611 \\
riscv64 & 261095 & 255.98 & 1.117 & 2.611 \\
\bottomrule
\end{tabular}
\smallskip\\
{\footnotesize \textbf{Determinism result.}
  The original 2026-06-11 aarch64 and riscv64 runs produced byte-for-byte
  identical CSV output across all 261\,095 rows (MD5 checksums matched),
  demonstrating that two architectures with distinct register files, memory
  models, and interrupt controllers produce identical observable physics.
  The clean aarch64 re-run (2026-06-12) diverges from riscv64 in heat
  columns (\texttt{hot\_word\_count}, \texttt{avg\_word\_heat\_q48}) from
  tick 34\,001 onward, while all attractor metrics (execution rate, window
  width, temporal trust, timing variance) remain identical.  Heat accumulation
  has a small run-to-run variation; the compudynamic attractor does not.
  See \S\ref{sec:disc_heat_variation}.
  amd64 differs by three rows (261\,098 vs 261\,095) due to x86-64 APIC
  scheduling; this has no effect on any reported statistic.}
\end{table}

The first rows of every architecture are identical (deterministic
POST + boot sequence), confirming that early divergence between runs is
precisely zero.

%% ═════════════════════════════════════════════════════════════════════════════
\section{The Adaptive Window Attractor}
\label{sec:attractor}

\subsection{Poincaré Map --- The Centrepiece Result}

The SSM's rolling-window width is an integer restricted to powers of two in
the range $[256,\, 4096]$ cells.  At each heartbeat tick the L8 Jacquard
mode selector may grow, shrink, or hold the window according to the
entropy/CV/decay state.  Plotting $w_{n+1}$ against $w_n$ --- the
discrete-time Poincaré map --- reveals the attractor structure.

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{poincare_light}
  \caption{Light theme}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{poincare_dark}
  \caption{Dark theme}
\end{subfigure}
\caption{Window-width Poincaré map ($\log_2$ scale, all architectures combined,
  783\,288 ticks).
  Each pixel encodes tick density.  The dominant off-diagonal ridge corresponds
  to the breathing cycle $256 \to 512 \to 1024 \to 2048 \to 4096 \to 2048 \to
  1024 \to 512 \to 256$; the diagonal cluster near $(256, 256)$ is the
  saturation floor.}
\label{fig:poincare}
\end{figure}

The map shows a clear \emph{limit cycle}: the window never stalls at a fixed
point but continuously traverses the five-point orbit.  This is the
compudynamic breathing cycle --- the attractor that the SSM seeks and
maintains regardless of workload configuration.

\subsection{Window Time Series}

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{window_ts_light}
  \caption{Light theme}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{window_ts_dark}
  \caption{Dark theme}
\end{subfigure}
\caption{Rolling-window width over the session per architecture.
  All three ISAs trace the identical breathing pattern through the full
  2.611-second (261\,000-tick) run.  The cycle period and amplitude are
  architecture-independent.}
\label{fig:window_ts}
\end{figure}

Figure~\ref{fig:window_ts} confirms that the breathing cycle is preserved
across ISAs.  The cycle period and amplitude are architecture-independent,
consistent with the hypothesis that the attractor is a property of the SSM
algorithm.

%% ═════════════════════════════════════════════════════════════════════════════
\section{Thermal Dynamics}
\label{sec:thermal}

\subsection{Heat Accumulation}

``Heat'' in the SSM context quantifies the weighted execution frequency of
the hottest dictionary word.  Figure~\ref{fig:heat_ts} shows how heat
accumulates over the run.

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{heat_ts_light}
  \caption{Light theme}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{heat_ts_dark}
  \caption{Dark theme}
\end{subfigure}
\caption{Average word heat over the run, sampled at every 5th tick.
  amd64 shows elevated mean heat (4.63) versus aarch64 (0.50) and riscv64
  (0.35); heat values have a small run-to-run variation (see
  \S\ref{sec:disc_heat_variation}); all three reach a peak of approximately 9.3--9.4 before the decay
  loop intervenes.  The amd64 thermal offset is consistent with x86-64 CISC
  instruction density: longer TCG traces per FORTH word produce greater
  per-word heat accumulation.}
\label{fig:heat_ts}
\end{figure}

\subsection{Thermal Phase Portrait}

The discrete-time derivative $\Delta h_n = h_{n+1} - h_n$ placed against
$h_n$ gives the thermal phase portrait --- the bare-metal analogue of the L8
attractor HR vs $\Delta$HR portrait from the hosted-VM experiment.

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{thermal_portrait_light}
  \caption{Light theme}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{thermal_portrait_dark}
  \caption{Dark theme}
\end{subfigure}
\caption{Thermal phase portrait: heat $h_n$ vs $\Delta h_n$ per architecture
  (log-density scale, 783\,288 ticks).  The accumulation arm at positive
  $\Delta h$ and the sharp reflection trace the thermal charge/discharge
  cycle.  amd64's rightward displacement along the heat axis reflects
  elevated steady-state heat due to CISC trace length, not a difference
  in the attractor topology.}
\label{fig:thermal}
\end{figure}

%% ═════════════════════════════════════════════════════════════════════════════
\section{Secondary Metrics}
\label{sec:secondary}

\subsection{Hot-Word Count Evolution}

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{hot_words_ts_light}
  \caption{Light theme}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{hot_words_ts_dark}
  \caption{Dark theme}
\end{subfigure}
\caption{Number of ``hot'' dictionary words (execution heat $>$ threshold)
  per tick, sampled at every 5th tick.  amd64 stabilises near 75.5~words;
  aarch64 near 27.8~words; riscv64 near 27.6~words.  The amd64 offset
  reflects higher per-word heat accumulation under x86-64 TCG (see
  \S\ref{sec:thermal}), not a difference in the attractor.}
\label{fig:hot_words}
\end{figure}

\subsection{Execution-Rate Distribution}

Figure~\ref{fig:exec_rate} presents the per-architecture execution-rate
distribution drawn from all 783\,288 ticks.  Every architecture achieves a
grand mean of \(255.98 \pm 1.11\)--\(1.12\) (SD) FORTH word executions per
heartbeat tick.  The inter-architecture spread is indistinguishable from the
intra-architecture noise floor.  The SSM's window-width feedback loop
normalises throughput to the tick boundary: when execution outpaces the
10\,\(\mu\)s target the window grows, increasing per-tick work; when it lags
the window shrinks.  The null result in \S\ref{sec:stats} confirms this formally.

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{exec_rate_light}
  \caption{Light theme}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{exec_rate_dark}
  \caption{Dark theme}
\end{subfigure}
\caption{Distribution of executions per tick by architecture
  (all 783\,288 ticks).  Grand mean 255.98, SD $\approx$1.11, across all
  three ISAs.  The distributions are statistically indistinguishable
  (\(F_{2,783285}\approx 0\), \(p=1.000\)).}
\label{fig:exec_rate}
\end{figure}

\subsection{Time-Trust and Variance}

The ARMv8 Generic Timer (aarch64) and RISC-V CLINT (riscv64) deliver their
10\,\(\mu\)s heartbeat ticks with \texttt{time\_trust\_q48} = 65\,536 (decoded:
$\bar{t}=1.0000$) and \texttt{variance\_q48} = 0 across every tick of the
30-replicate campaign.  The x86-64 APIC emulation, however, introduces a small
but statistically significant calibration residual in the post-boot region
(ticks $> 26\,251$, $n = 234\,847$): $\bar{t} = 0.9947$,
$\bar{\sigma}^2 = 9.5 \times 10^{-3}$.  This residual is absent from the RISC
targets, which record perfect trust and zero variance throughout.

The inter-architecture Kruskal--Wallis test confirms the divergence is
real and substantial (\(H = 667\,664\), $df = 2$, $p < 2.2 \times 10^{-16}$)
for both \texttt{time\_trust} and \texttt{variance}.  Critically, this divergence
is \textbf{attractor-neutral}: window width and execution rate are identical across
all three ISAs regardless of timer quality.  The APIC residual is a property of
the x86-64 TCG timer implementation, not of the SSM algorithm.

Figure~\ref{fig:trust_variance} shows kernel density estimates on the
post-boot region ($> 26\,251$ ticks per architecture).  The aarch64 and riscv64
densities collapse to a point mass at trust $= 1.0$; the amd64 density is
displaced leftward to $\approx 0.9947$ with a small spread.

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{trust_variance_light}
  \caption{Light theme}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \includegraphics[width=\textwidth]{trust_variance_dark}
  \caption{Dark theme}
\end{subfigure}
\caption{Kernel density of temporal trust (upper) and Q48.16 variance (lower)
  in the post-boot region ($>26\,251$ ticks).  aarch64 and riscv64 record
  perfect trust ($= 1.0$) and zero variance; amd64 shows the x86-64 APIC
  calibration residual ($\bar{t}=0.9947$, $\bar{\sigma}^2=9.5\times10^{-3}$).
  The residual does not perturb the attractor.}
\label{fig:trust_variance}
\end{figure}

%% ═════════════════════════════════════════════════════════════════════════════
\section{Statistical Analysis}
\label{sec:stats}

\subsection{Execution Rate: Null Result}

We tested whether mean executions per tick differs by architecture using
one-way ANOVA on the full 783\,288-row dataset:

\[
  F_{2,\,783285} \approx 0, \quad p = 1.000
\]

This zero F-statistic is the strongest finding in the paper:
\textbf{the FORTH interpreter executes at statistically identical rates
across three radically different ISAs.}  The between-group variance is
indistinguishable from zero --- the SSM's compudynamic invariant holds
absolutely across silicon.

\subsection{Timer Quality: Architecture-Specific Residual}

Kruskal--Wallis rank tests on the post-boot region (ticks $> 26\,251$,
$n = 234\,847$ per architecture) detect a significant between-architecture
difference in both temporal trust and timing variance:

\begin{table}[h!]
\centering
\caption{Kruskal--Wallis tests --- post-boot divergent region}
\label{tab:kw}
\begin{tabular}{llSl}
\toprule
Metric & {$H$} & {$df$} & {$p$-value} \\
\midrule
\texttt{time\_trust}  & 667\,664 & 2 & $< 2.2 \times 10^{-16}$ \\
\texttt{variance}     & 667\,664 & 2 & $< 2.2 \times 10^{-16}$ \\
\bottomrule
\end{tabular}
\end{table}

The large $H$ statistic arises because aarch64 and riscv64 produce
\emph{identical} trust and variance values (point masses at $1.0$ and $0$,
respectively), while amd64 produces a distinct distribution centred at
$\bar{t}=0.9947$ with $\bar{\sigma}^2=9.5\times10^{-3}$ --- the x86-64 APIC
calibration residual.  This divergence is real and statistically unambiguous, and
\textbf{attractor-neutral}: execution rate and window width are identical across
all three architectures regardless.

The SSM's \emph{compudynamic} invariant (execution rate, window width, attractor
topology) holds at 0\,\% variance.  Timer quality is a property of the hardware
timer implementation, not of the algorithm, and does not propagate to the
attractor.

\subsection{Architecture Summary}

\begin{table}[h!]
\centering
\caption{Per-architecture summary statistics (30 replicates, 261\,095--261\,098 ticks)}
\label{tab:summary}
\sisetup{round-mode=places, round-precision=3}
\begin{tabular}{lSSSSS}
\toprule
{Architecture} &
  {Mean heat} & {Max heat} &
  {Mean window} &
  {Mean trust} &
  {Hot words} \\
\midrule
amd64   & 4.634 & 9.270 & 256.7 & 0.9946 & 75.5 \\
aarch64 & 0.498 & 9.308 & 256.7 & 1.0000 & 27.8 \\
riscv64 & 0.349 & 9.303 & 256.7 & 1.0000 & 27.6 \\
\bottomrule
\end{tabular}
\end{table}

Mean window width (256.7 cells) is identical across all three architectures.
Temporal trust differs: amd64 records $\bar{t}=0.9946$ due to the x86-64
APIC calibration residual; aarch64 and riscv64 record perfect trust
($\bar{t}=1.0000$).  The heat asymmetry between amd64 and the RISC targets
is discussed in \S\ref{sec:disc_hot}.

%% ═════════════════════════════════════════════════════════════════════════════
\section{Discussion}
\label{sec:discussion}

\subsection{Compudynamic Invariance}

The central claim of Compudynamics is that a sufficiently rich adaptive runtime
will converge to an attractor that is determined by the algorithm's intrinsic
dynamics rather than by the hardware substrate.  The present results provide
strong empirical support for this claim.

The breathing cycle $256 \to 512 \to 1024 \to 2048 \to 4096 \to \cdots$
is a discrete limit cycle in phase space.  Its period (approximately 128--256
ticks at 10\,\(\mu\)s resolution) is stable across three radically different
micro-architectures emulated in software.  The Poincaré map
(Figure~\ref{fig:poincare}) shows the same ridge structure in all three colour
channels.

\subsection{The Null Result on Execution Rate}

The ANOVA null result ($F_{2,783285}\approx 0$, $p = 1.000$) deserves emphasis.
In ordinary benchmarking, one would expect different ISAs to produce different
instruction throughputs.  Here, the \emph{executions per heartbeat tick} is
held equal not by throttling but by the SSM's feedback mechanism: when the
system is running faster than expected the window grows (increasing work per
tick) and when it is running slower the window shrinks (reducing work per
tick).  The zero F-statistic reflects a between-group variance that is
indistinguishable from zero over 783\,288 observations.  The null result is
the SSM working correctly.

\subsection{APIC Residual: Hardware Divergence Without Algorithmic Consequence}

The 30-replicate clean campaign resolves the timer-quality question definitively.
The ARMv8 Generic Timer (aarch64) and RISC-V CLINT (riscv64) deliver perfect
trust ($\bar{t}=1.0000$, $\bar{\sigma}^2=0$) throughout.  The x86-64 APIC
emulation introduces a small calibration residual in the post-boot region:
$\bar{t}=0.9947$, $\bar{\sigma}^2=9.5\times10^{-3}$ --- a 0.53\,\%
trust deficit relative to the nominal value.

Critically, this residual is \emph{attractor-neutral}.  The Poincaré map
(Figure~\ref{fig:poincare}), the breathing cycle period, and the execution rate
are identical across all three ISAs (see §\ref{sec:stats}) despite the APIC
deficit.  The SSM absorbs the APIC timing imperfection via the L7 adaptive
heartrate and L8 Jacquard mode controls without perturbing the attractor.  This
robustness is itself a positive result: the compudynamic invariant is insensitive
to sub-percent timer noise.

\subsection{Hot-Word Count Asymmetry and Saturation}
\label{sec:disc_hot}

Dividing the session into ten equal deciles reveals that the asymmetry
between amd64 and the RISC targets is not uniform over time.  In the first
decile (POST + boot, tick $\leq 26{,}110$) all three architectures start
similarly: amd64 36.2, aarch64/riscv64 $\approx$39.0 hot words.  By decile 2
amd64 reaches $\approx$75.5 words and holds near that value for all remaining
deciles --- config-invariant, saturated above the heat threshold for the
entire DoE run.  aarch64 and riscv64, by contrast, oscillate with the DoE
configuration being executed: 70.9 $\to$ 23.9 $\to$ 28.2 $\to$ 42.9 $\to$
2.6 $\to$ 49.3 $\to$ 10.3 $\to$ 32.9 $\to$ 17.5 $\to$ 0.09, ending near
zero as heat decays in the final replicate batch.

The implication is two-fold.  First, amd64's CISC TCG trace length acts as a
heat amplifier that saturates the hot-word count above any per-configuration
variation; the RISC targets remain below saturation and therefore show genuine
experimental sensitivity to the DoE factors.  Second, and critically, the
hot-word count asymmetry does \emph{not} perturb the attractor: window width
and execution rate are identical across all three architectures regardless of
hot-word count.  The compudynamic invariant holds whether the substrate is
thermally saturated (amd64) or thermally responsive (aarch64/riscv64).
The saturation behaviour is a hypothesis for follow-on physical-hardware work.

\subsection{Heat Accumulation: Run-to-Run Variation}
\label{sec:disc_heat_variation}

A clean re-run of the aarch64 30-replicate campaign (2026-06-12) diverges from
the original run (2026-06-11) in heat columns (\texttt{hot\_word\_count},
\texttt{avg\_word\_heat\_q48}) beginning at tick 34\,001, while all attractor
metrics remain identical.  This finding has two implications.

First, the \emph{original} byte-identity between aarch64 and riscv64 was a
single-session observation, not a universal invariant.  Heat accumulation is
sensitive to small non-deterministic factors (e.g.\ QEMU timer jitter, host
scheduling) that do not affect the attractor.

Second, and more importantly, the compudynamic attractor \emph{is} invariant
across both the original and re-run aarch64 data: execution rate, window width,
temporal trust, and timing variance are bit-exact between the two sessions.
The SSM converges to the same attractor regardless of which thermal path is taken
through the heat-accumulation state space.  This is a stronger robustness result
than byte-identity: it shows the attractor is reachable from multiple initial
heat trajectories.

\subsection{Limitations}
\label{sec:disc_independence}

\begin{enumerate}[noitemsep]
  \item \textbf{QEMU TCG emulation.}  All three architectures were tested
    under software emulation, not physical hardware.  TCG determinism
    strengthens the invariance result (eliminating silicon noise) but means
    physical-hardware clock jitter, cache effects, and branch predictor
    behaviour are not yet characterised.  Physical campaigns are planned for
    Milestone M10.
  \item \textbf{Compressed DoE matrix.}  The matrix is $2^4 = 16$
    configurations; the hosted experiment used $2^7 = 128$.  The design
    suffices for cross-ISA comparison but does not sweep the full SSM
    parameter space.
  \item \textbf{aarch64/riscv64 statistical independence.}  The original
    aarch64 and riscv64 runs produced byte-identical output; the clean
    aarch64 re-run diverges in heat metrics (see \S\ref{sec:disc_heat_variation}).
    In either case, the attractor metrics are identical, and any between-ISA
    inference on those metrics treats effectively the same distribution.
    Future campaigns should vary the PRNG seed per architecture to ensure
    fully independent replicates.
\end{enumerate}

%% ═════════════════════════════════════════════════════════════════════════════
\section{Conclusion}
\label{sec:conclusion}

This paper presents the first 30-replicate bare-metal validation of the
StarForth SSM across three instruction-set architectures.  Key findings:

\begin{enumerate}[noitemsep]
  \item \textbf{Compudynamic invariance confirmed.}  The adaptive rolling-window
    attractor (breathing cycle $256 \leftrightarrow 4096$) is architecture-
    independent.  Mean window width is identical across amd64, aarch64, and
    riscv64 at 256.7 cells.

  \item \textbf{Execution rate is architecture-neutral.}  One-way ANOVA over
    783\,288 ticks finds no significant difference ($F_{2,783285}\approx 0$,
    $p=1.000$) --- the strongest possible null result.

  \item \textbf{Compudynamic variance is zero; timer variance is not.}
    Execution rate, window width, and attractor topology are identical across
    all three ISAs.  Timer quality diverges: the x86-64 APIC emulation leaves
    a 0.53\,\% trust residual ($\bar{t}=0.9947$) absent from aarch64 and
    riscv64 (KW $H=667\,664$, $p<2.2\times10^{-16}$).  The residual is
    attractor-neutral.

  \item \textbf{LithosAnanke boots successfully on all three ISAs.}  All
    three QEMU acceptance runs completed POST (936+ tests), reached the
    \texttt{ok>} REPL prompt, and emitted valid DoE CSV data.
\end{enumerate}

These results establish the SSM as a genuinely portable computational-physics
engine.  The compudynamic attractor is a feature of the algorithm, not of any
particular silicon.

%% ═════════════════════════════════════════════════════════════════════════════
\appendix
\section{Reproducibility}
\label{app:repro}

\subsection{Build}

\begin{verbatim}
  git clone https://github.com/rajames440/starforth
  git checkout lithosananke
  make -f Makefile.starkernel ARCH=amd64   clean qemu DOE_REPS=30
  make -f Makefile.starkernel ARCH=aarch64 clean qemu DOE_REPS=30
  make -f Makefile.starkernel ARCH=riscv64 clean qemu DOE_REPS=30
\end{verbatim}

CSV output appears on the QEMU serial stream and is captured automatically
to \texttt{experiments/bare\_metal/runs/}.

\subsection{Analysis}

\begin{verbatim}
  cd experiments/bare_metal/analysis
  Rscript analyse_bare_metal.R
  python3 svg_to_pdf.py
  cd report && pdflatex bare_metal_doe_report.tex
              pdflatex bare_metal_doe_report.tex
\end{verbatim}

R packages required: \texttt{dplyr}, \texttt{tidyr}, \texttt{ggplot2},
\texttt{viridis}, \texttt{svglite}, \texttt{patchwork}, \texttt{scales}.
System dependency: \texttt{librsvg2-bin} (provides \texttt{rsvg-convert}).

\subsection{Data Files}

\begin{itemize}[noitemsep]
  \item \texttt{experiments/bare\_metal/runs/doe-amd64-20260612-110212.csv}
    \quad (261\,098 rows)
  \item \texttt{experiments/bare\_metal/runs/doe-aarch64-20260612-132412.csv}
    \quad (261\,095 rows)
  \item \texttt{experiments/bare\_metal/runs/doe-riscv64-20260611-191335.csv}
    \quad (261\,095 rows)
\end{itemize}

All three files are tracked in Git LFS.

\subsection{Acceptance Logs}

\begin{itemize}[noitemsep]
  \item \texttt{logs2/qemu-amd64-20260612-110212.log}
  \item \texttt{logs2/qemu-aarch64-20260611-202955.log}
  \item \texttt{logs2/qemu-riscv64-20260611-203104.log}
\end{itemize}

%% ── End ───────────────────────────────────────────────────────────────────────
\end{document}
