# Always ignore Makefiles and build detritus
(^|/)?Makefile($|[.].*)
(^|/)?build($|/)

# Never include local maint machinery
(^|/)?maint($|/)

# Repo-specific files (each repo maintains its own)
(^|/)?\.gitignore$
(^|/)?\.github($|/)

# (Starship-specific assets blacklist only matters in reverse direction,
# but harmless to leave here.)
(^|/)?Control($|/)
(^|/)?LICENSE\.spdx$
